# Python + VS Code + Claude Code - Быстрый старт

## 🚀 Создание проекта (1 команда!)

```powershell
# Базовый проект
.\Scripts\create-python-project.ps1 my-project

# FastAPI (API/Backend)
.\Scripts\create-python-project.ps1 my-api -Template fastapi

# Flask (Web)
.\Scripts\create-python-project.ps1 my-web -Template flask

# Data Science
.\Scripts\create-python-project.ps1 my-analysis -Template data-science

# CLI приложение
.\Scripts\create-python-project.ps1 my-cli -Template cli
```

Скрипт автоматически:
- ✅ Создаёт структуру проекта
- ✅ Настраивает виртуальное окружение
- ✅ Устанавливает зависимости
- ✅ Создаёт конфигурацию VS Code
- ✅ Настраивает отладку
- ✅ Инициализирует Git

---

## 📁 Что создаётся

```
my-project/
├── .venv/              # Виртуальное окружение
├── .vscode/            # Настройки VS Code
│   ├── settings.json   # Python, форматирование, тесты
│   └── launch.json     # Отладка
├── src/
│   ├── __init__.py
│   └── main.py         # Готовый код по шаблону
├── tests/
│   ├── __init__.py
│   └── test_main.py    # Готовые тесты
├── .gitignore          # Python gitignore
├── .env.example        # Пример конфигурации
├── requirements.txt    # Зависимости
├── requirements-dev.txt # Dev зависимости
└── README.md           # Документация
```

---

## 🔧 Открытие в VS Code

```powershell
cd D:\DevTools\Database\my-project
code .
```

**VS Code автоматически:**
- Обнаружит Python интерпретатор (`.venv`)
- Настроит форматирование (Black)
- Настроит линтинг (Flake8)
- Настроит тесты (pytest)

---

## 🎯 Работа с Claude Code

### 1. Открыть чат с Claude

**Способ 1:** Нажмите `Ctrl+Shift+P` → введите `Claude: Chat`

**Способ 2:** Кликните иконку Claude на боковой панели

### 2. Примеры запросов

**Создание кода:**
```
Создай FastAPI endpoint для регистрации пользователя с валидацией email
```

**Рефакторинг:**
```
Выдели → Ctrl+Shift+P → Claude: Edit
Оптимизируй эту функцию и добавь type hints
```

**Тесты:**
```
Напиши pytest тесты для функции calculate_total с edge cases
```

**Объяснение:**
```
Объясни как работает этот декоратор
```

**Исправление:**
```
Почему этот код вызывает ошибку? Исправь
```

### 3. Claude видит автоматически

- ✅ Открытые файлы
- ✅ Структуру проекта
- ✅ requirements.txt
- ✅ README.md
- ✅ Выделенный код

### 4. Лучшие практики

**DO:**
- Держите открытыми релевантные файлы
- Используйте понятные имена переменных
- Добавляйте комментарии к сложному коду
- Создавайте хороший README.md

**DON'T:**
- Не давайте расплывчатые запросы
- Не полагайтесь только на Claude (проверяйте код!)

---

## ▶️ Запуск проекта

### 1. Активировать окружение

**PowerShell:**
```powershell
.\.venv\Scripts\activate.ps1
```

**CMD:**
```cmd
.\.venv\Scripts\activate.bat
```

### 2. Запустить приложение

```bash
# Базовый проект
python src/main.py

# FastAPI (с авто-перезагрузкой)
uvicorn src.main:app --reload

# Flask
flask --app src.main run --debug
```

### 3. В VS Code - нажмите `F5` (отладка)

---

## 🧪 Тестирование

### Запуск из терминала

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=src

# Конкретный файл
pytest tests/test_main.py

# В watch mode (при изменениях)
pytest-watch
```

### В VS Code

1. Откройте панель **Testing** (иконка колбы слева)
2. Кликните **Refresh Tests**
3. Запускайте тесты кликом

**Или нажмите `Ctrl+Shift+P` → `Python: Run All Tests`**

---

## 🐛 Отладка

### 1. Установите breakpoint

- Кликните слева от номера строки (красная точка)
- Или курсор на строку + `F9`

### 2. Запустите отладку

- Нажмите `F5`
- Или: Run → Start Debugging
- Или: Кликните зелёную стрелку вверху

### 3. Навигация

- `F5` - Continue
- `F10` - Step Over (следующая строка)
- `F11` - Step Into (войти в функцию)
- `Shift+F11` - Step Out (выйти из функции)
- `Shift+F5` - Stop

### 4. Debug Console

Внизу появится консоль:
- Проверяйте переменные: `print(my_var)`
- Вызывайте функции: `calculate(10, 20)`
- Выполняйте код

---

## 📦 Управление зависимостями

### Установка новых пакетов

```bash
# Активируйте venv
.\.venv\Scripts\activate.ps1

# Установите пакет
pip install package-name

# Обновите requirements.txt
pip freeze > requirements.txt
```

### Установка всех зависимостей

```bash
# Production
pip install -r requirements.txt

# Development (включает production)
pip install -r requirements-dev.txt
```

---

## 🎨 Форматирование кода

### Автоматически (в VS Code)

При сохранении файла (`Ctrl+S`) код автоматически форматируется

### Вручную

```bash
# Отформатировать всё
black src tests

# Проверить без изменений
black src tests --check

# Конкретный файл
black src/main.py
```

---

## 🔍 Линтинг

```bash
# Flake8 (проверка стиля)
flake8 src tests

# Mypy (проверка типов)
mypy src

# В VS Code - ошибки показываются автоматически
```

---

## 📊 Полезные команды

### Python

```bash
# Версия Python
python --version

# Список установленных пакетов
pip list

# Информация о пакете
pip show package-name

# Обновить пакет
pip install --upgrade package-name

# Удалить пакет
pip uninstall package-name
```

### Git

```bash
# Статус
git status

# Добавить всё
git add .

# Коммит
git commit -m "Message"

# История
git log --oneline
```

---

## 🔧 Горячие клавиши VS Code

### Общие

- `Ctrl+P` - Быстрый поиск файлов
- `Ctrl+Shift+P` - Command Palette
- `Ctrl+`` - Открыть/закрыть терминал
- `Ctrl+B` - Скрыть/показать боковую панель

### Python

- `Shift+Enter` - Запустить выделенный код в терминале
- `F5` - Запуск отладки
- `Ctrl+F5` - Запуск без отладки
- `F9` - Toggle breakpoint
- `F12` - Go to Definition
- `Shift+F12` - Find All References

### Редактирование

- `Ctrl+/` - Закомментировать/раскомментировать
- `Alt+↑/↓` - Переместить строку
- `Ctrl+D` - Выделить следующее вхождение
- `Ctrl+Shift+L` - Выделить все вхождения
- `Alt+Click` - Множественные курсоры

---

## 📚 Шаблоны кода

### Быстрая функция

```python
def function_name(param: type) -> return_type:
    """Docstring."""
    pass
```

### Класс

```python
class MyClass:
    """Class docstring."""

    def __init__(self, param: type):
        """Initialize."""
        self.param = param

    def method(self) -> return_type:
        """Method docstring."""
        pass
```

### Async функция

```python
async def async_function() -> None:
    """Async function."""
    await some_async_call()
```

---

## 🆘 Решение проблем

### Python интерпретатор не найден

1. `Ctrl+Shift+P`
2. `Python: Select Interpreter`
3. Выберите `.venv\Scripts\python.exe`

### Модуль не найден

```bash
# Убедитесь что venv активирован
.\.venv\Scripts\activate.ps1

# Переустановите зависимости
pip install -r requirements-dev.txt
```

### Тесты не обнаружены

1. Откройте панель Testing
2. Кликните "Configure Python Tests"
3. Выберите pytest
4. Укажите папку tests

### Claude Code не работает

1. Проверьте что расширение установлено
2. Перезагрузите VS Code
3. Проверьте интернет соединение

---

## 🎓 Полная документация

Читайте: [PYTHON_PROJECT_GUIDE.md](Docs/PYTHON_PROJECT_GUIDE.md)

---

## 🎯 Примеры проектов

### FastAPI CRUD API

```powershell
.\Scripts\create-python-project.ps1 users-api -Template fastapi
cd Database\users-api
code .
```

Попросите Claude:
```
Создай CRUD endpoints для модели User с полями:
- id (UUID)
- email (EmailStr)
- username (str)
- created_at (datetime)

Добавь SQLAlchemy модели и Pydantic схемы
```

### Data Analysis проект

```powershell
.\Scripts\create-python-project.ps1 sales-analysis -Template data-science
cd Database\sales-analysis
code .
```

Попросите Claude:
```
Создай анализ продаж с:
1. Загрузкой CSV файла
2. Очисткой данных
3. Визуализацией трендов
4. Статистическим анализом
```

### CLI инструмент

```powershell
.\Scripts\create-python-project.ps1 file-manager -Template cli
cd Database\file-manager
code .
```

Попросите Claude:
```
Создай CLI для управления файлами с командами:
- list - показать файлы
- search - поиск файлов
- organize - организовать по типу
```

---

**Готово! Начинайте разработку!** 🚀

**Быстрый старт:**
```powershell
.\Scripts\create-python-project.ps1 my-first-project
cd Database\my-first-project
code .
```
