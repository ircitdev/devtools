# Jupyter Notebook - Руководство

## 🎯 Что такое Jupyter Notebook?

**Jupyter Notebook** - это веб-приложение для создания интерактивных документов с кодом, визуализацией и текстом.

### Основные возможности:
- ✅ Выполнение кода по частям (ячейками)
- ✅ Визуализация результатов сразу под кодом
- ✅ Создание графиков и диаграмм
- ✅ Документирование с помощью Markdown
- ✅ Математические формулы (LaTeX)
- ✅ Экспорт в HTML, PDF, слайды

## 🚀 Запуск Jupyter

У вас уже установлен Jupyter! Запустите:

### Jupyter Notebook (классический интерфейс)
```powershell
.\Scripts\start-jupyter.ps1
```

### JupyterLab (современный интерфейс, рекомендуется)
```powershell
.\Scripts\start-jupyterlab.ps1
```

Jupyter откроется в браузере автоматически на `http://localhost:8888`

## 📁 Структура

### Notebook файлы (.ipynb)
- Содержат код, текст, результаты
- Можно открывать и редактировать
- Хранятся в формате JSON

### Где хранить notebooks?
Создайте папку для проектов:
```powershell
mkdir D:\DevTools\Database\notebooks
cd D:\DevTools\Database\notebooks
.\..\..\Scripts\start-jupyter.ps1
```

## 📝 Типы ячеек

### 1. Code (Код)
```python
# Выполняемый Python код
x = 10
print(x * 2)
```

### 2. Markdown (Текст)
```markdown
# Заголовок
**Жирный текст**
*Курсив*
- Список
```

### 3. Raw (Сырой текст)
Текст без обработки, используется редко

## ⌨️ Горячие клавиши

### Command Mode (ESC)
| Клавиша | Действие |
|---------|----------|
| `A` | Добавить ячейку выше |
| `B` | Добавить ячейку ниже |
| `D, D` | Удалить ячейку |
| `M` | Преобразовать в Markdown |
| `Y` | Преобразовать в Code |
| `C` | Копировать ячейку |
| `V` | Вставить ячейку |
| `Z` | Отменить удаление |

### Edit Mode (ENTER)
| Клавиша | Действие |
|---------|----------|
| `Shift + Enter` | Выполнить и перейти к следующей |
| `Ctrl + Enter` | Выполнить текущую |
| `Alt + Enter` | Выполнить и создать новую ниже |
| `Tab` | Автодополнение |
| `Shift + Tab` | Показать документацию |

## 🎨 Примеры использования

### Анализ данных
```python
import pandas as pd
import matplotlib.pyplot as plt

# Загрузить данные
df = pd.read_csv('data.csv')

# Построить график
df.plot(kind='bar')
plt.show()
```

### Визуализация
```python
import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0, 10, 100)
plt.plot(x, np.sin(x))
plt.title('График sin(x)')
plt.show()
```

### Документация
```markdown
# Мой проект

## Описание
Этот проект анализирует данные о продажах.

## Результаты
- Средняя продажа: $1000
- Максимум: $5000
```

## 🔮 Магические команды

### Встроенные команды (начинаются с %)

```python
# Измерить время выполнения
%timeit sum(range(1000))

# Показать все переменные
%whos

# Выполнить shell команду
!ls -la

# Загрузить код из файла
%load script.py

# Сохранить ячейку в файл
%%writefile output.py
def hello():
    print("Hello!")

# Отображение графиков в notebook
%matplotlib inline
```

### Другие полезные команды

```python
# Автоматическое обновление импортированных модулей
%load_ext autoreload
%autoreload 2

# Отладка
%debug

# История команд
%history
```

## 📊 Работа с графиками

### Matplotlib
```python
%matplotlib inline
import matplotlib.pyplot as plt

plt.figure(figsize=(10, 5))
plt.plot([1, 2, 3, 4], [1, 4, 9, 16])
plt.show()
```

### Seaborn
```python
import seaborn as sns
import pandas as pd

df = pd.DataFrame({'x': [1, 2, 3, 4], 'y': [1, 4, 9, 16]})
sns.lineplot(data=df, x='x', y='y')
```

### Plotly (интерактивные графики)
```python
import plotly.express as px

fig = px.line(x=[1, 2, 3, 4], y=[1, 4, 9, 16])
fig.show()
```

## 📤 Экспорт

### В HTML
```bash
jupyter nbconvert --to html notebook.ipynb
```

### В PDF
```bash
jupyter nbconvert --to pdf notebook.ipynb
```

### В Python скрипт
```bash
jupyter nbconvert --to script notebook.ipynb
```

### В слайды
```bash
jupyter nbconvert --to slides notebook.ipynb --post serve
```

## 🛠️ Расширения

### Установка nbextensions
```bash
pip install jupyter_contrib_nbextensions
jupyter contrib nbextension install --user
```

### Популярные расширения:
- Table of Contents - оглавление
- Collapsible Headings - сворачиваемые секции
- Code folding - сворачивание кода
- ExecuteTime - время выполнения ячеек

## 💡 Best Practices

### 1. Организация
- Используйте заголовки для структуры
- Группируйте связанный код
- Добавляйте описания

### 2. Именование
- Понятные имена переменных
- Комментарии к сложным участкам
- Описание функций

### 3. Визуализация
- Подписывайте оси
- Добавляйте заголовки
- Используйте легенды

### 4. Производительность
- Не загружайте большие данные многократно
- Используйте `%%timeit` для оптимизации
- Очищайте вывод больших объектов

## 🎓 Примеры проектов

### 1. Анализ данных
```python
# Загрузить данные
import pandas as pd
df = pd.read_csv('sales.csv')

# Базовая статистика
print(df.describe())

# Визуализация
df.groupby('month')['sales'].sum().plot(kind='bar')
```

### 2. Machine Learning
```python
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression

# Подготовка данных
X_train, X_test, y_train, y_test = train_test_split(X, y)

# Обучение модели
model = LinearRegression()
model.fit(X_train, y_train)

# Оценка
score = model.score(X_test, y_test)
print(f'Accuracy: {score}')
```

### 3. API интеграция
```python
import requests

# Запрос к API
response = requests.get('https://api.example.com/data')
data = response.json()

# Обработка
df = pd.DataFrame(data)
df.head()
```

## 🆘 Решение проблем

### Jupyter не запускается
```powershell
# Переустановить
.\Scripts\quick-pip.ps1 install --upgrade jupyter
```

### Kernel died (ядро умерло)
- Перезапустите kernel: Kernel > Restart
- Очистите вывод: Cell > All Output > Clear
- Проверьте память

### Ячейка выполняется бесконечно
- Прервите: Kernel > Interrupt
- Или нажмите кнопку ⬛ (Stop)

### Не видно графиков
```python
%matplotlib inline
import matplotlib.pyplot as plt
```

## 📚 Ресурсы

- [Официальная документация](https://jupyter.org/documentation)
- [Jupyter Notebook Tutorial](https://jupyter-notebook.readthedocs.io/)
- [JupyterLab Documentation](https://jupyterlab.readthedocs.io/)
- [IPython Documentation](https://ipython.readthedocs.io/)

## 🎯 Пример notebook

Откройте готовый пример:
```powershell
cd D:\DevTools\Database
.\Scripts\start-jupyter.ps1
# Откройте example_notebook.ipynb
```

---

**Jupyter Notebook готов к использованию!** 🚀

Запустите: `.\Scripts\start-jupyter.ps1` или `.\Scripts\start-jupyterlab.ps1`
