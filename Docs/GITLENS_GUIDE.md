# GitLens - Руководство по использованию

> Расширение для VS Code, которое превращает Git в мощный инструмент разработки

## 📦 Установка

### Способ 1: Через VS Code (рекомендуется)

1. Откройте VS Code
2. Нажмите `Ctrl+Shift+X` (Extensions)
3. Найдите "GitLens — Git supercharged"
4. Нажмите **Install**

### Способ 2: Через командную строку

```powershell
code --install-extension eamodio.gitlens
```

---

## ⚙️ Настройка для DevTools

После установки GitLens автоматически работает, но можно настроить под себя.

### Рекомендуемые настройки

Откройте VS Code Settings (`Ctrl+,`) и добавьте:

```json
{
  // GitLens - основные настройки
  "gitlens.currentLine.enabled": true,
  "gitlens.currentLine.pullRequests.enabled": false,
  "gitlens.hovers.currentLine.over": "line",

  // Code Lens над функциями
  "gitlens.codeLens.enabled": true,
  "gitlens.codeLens.authors.enabled": false,
  "gitlens.codeLens.recentChange.enabled": true,

  // Inline blame аннотации
  "gitlens.blame.format": "${author}, ${date}",
  "gitlens.blame.highlight.enabled": true,

  // Статус бар
  "gitlens.statusBar.enabled": true,
  "gitlens.statusBar.alignment": "right",

  // График коммитов
  "gitlens.graph.layout": "editor",
  "gitlens.graph.minimap.enabled": true
}
```

### Минималистичная настройка (если хотите меньше информации)

```json
{
  "gitlens.currentLine.enabled": false,
  "gitlens.codeLens.enabled": false,
  "gitlens.hovers.enabled": true,
  "gitlens.statusBar.enabled": true
}
```

---

## 🎯 Основные функции

### 1. Git Blame (кто написал код)

**Inline аннотации:**
- Автоматически показывает автора и дату в конце текущей строки
- Серым цветом: `Author Name, 2 days ago`

**Как использовать:**
1. Просто переместите курсор на любую строку
2. Увидите автора и время изменения
3. Нажмите на аннотацию для деталей

**Горячие клавиши:**
- `Alt+B` - Toggle Git Blame
- Hover (наведение) - детали коммита

### 2. File History (история файла)

**Где найти:**
- В Explorer → правый клик на файл → "Open File History"
- Или откройте Timeline в боковой панели

**Что можно делать:**
- Просмотреть все коммиты для файла
- Сравнить любые две версии
- Откатиться к старой версии
- Посмотреть кто и когда менял

### 3. Commit Graph (граф коммитов)

**Как открыть:**
- `Ctrl+Shift+P` → "GitLens: Show Commit Graph"
- Или значок GitLens в боковой панели → Graph

**Возможности:**
- Визуальный граф всех коммитов и веток
- Фильтрация по автору, дате, ветке
- Сравнение коммитов
- Создание веток из любого коммита

### 4. Line History (история строки)

**Как использовать:**
1. Выделите строку или блок кода
2. Правый клик → "Show Line History"
3. Увидите все изменения именно этих строк

**Зачем это нужно:**
- Понять почему код написан так
- Найти когда появился баг
- Прочитать commit message для контекста

### 5. Compare (сравнение)

**Типы сравнения:**
- **Compare with HEAD** - текущая версия vs последний коммит
- **Compare with Working Tree** - коммит vs ваши изменения
- **Compare with Branch** - сравнить две ветки
- **Compare with Previous** - коммит vs предыдущий коммит

**Как использовать:**
1. Откройте Commit Graph
2. Правый клик на коммит → "Compare with..."
3. Выберите что с чем сравнивать

### 6. Search & Navigate

**Search Commits:**
- `Ctrl+Shift+P` → "GitLens: Search Commits"
- Поиск по сообщению, автору, файлу, hash

**Navigate:**
- `Alt+Left/Right` - навигация по истории файла
- `F7/Shift+F7` - следующий/предыдущий коммит

---

## 💡 Практические примеры

### Пример 1: Найти когда код сломался

**Сценарий:** Функция работала, теперь нет. Нужно найти изменение.

```powershell
# 1. Откройте файл с функцией
# 2. Выделите проблемную функцию
# 3. Правый клик → Show Line History
# 4. Просмотрите коммиты - найдите последнее изменение
# 5. Кликните на коммит - увидите что изменилось
```

### Пример 2: Понять зачем написан код

**Сценарий:** Непонятный код, нужен контекст.

```powershell
# 1. Наведите курсор на строку
# 2. Увидите inline blame: "John Doe, 3 weeks ago"
# 3. Нажмите на blame - откроется commit details
# 4. Прочитайте commit message и полный diff
```

### Пример 3: Code Review в VS Code

**Сценарий:** Проверить Pull Request.

```powershell
# 1. Откройте GitLens Graph
# 2. Найдите ветку PR (например, feature/new-api)
# 3. Правый клик → Compare with main
# 4. Просмотрите все изменения
# 5. Комментируйте прямо в VS Code
```

### Пример 4: Работа с DevTools скриптами

**Сценарий:** Скрипт create-python-project.ps1 работал, теперь ошибка.

```powershell
# 1. Откройте D:\DevTools\Scripts\create-python-project.ps1
# 2. Toggle Git Blame (Alt+B)
# 3. Увидите кто и когда менял каждую строку
# 4. Найдите недавние изменения
# 5. Откройте Line History для проблемной секции
```

---

## 🔥 Горячие клавиши

### Основные:

| Клавиша | Действие |
|---------|----------|
| `Alt+B` | Toggle Git Blame |
| `Ctrl+Shift+G B` | Show Git Blame в панели |
| `Ctrl+Shift+G H` | Show File History |
| `Ctrl+Shift+G S` | Search Commits |

### В редакторе:

| Клавиша | Действие |
|---------|----------|
| Hover | Детали коммита при наведении |
| Click на blame | Открыть commit details |
| `Alt+Left/Right` | Навигация по истории |

### Команды (Ctrl+Shift+P):

```
GitLens: Show Commit Graph
GitLens: Show File History
GitLens: Show Line History
GitLens: Compare with Branch
GitLens: Search Commits
GitLens: Toggle File Blame
```

---

## 🎨 UI Элементы GitLens

### 1. Inline Current Line Blame

Показывается в конце текущей строки:
```
    return result  // John Doe, 3 days ago • Added error handling
```

### 2. Status Bar

В правом нижнем углу:
```
[branch icon] main ↓2 ↑1  |  John Doe, 2 hours ago
```

### 3. Code Lens

Над функциями/классами:
```python
# John Doe, 3 days ago
# 5 authors, 12 changes
def create_project(name: str):
    ...
```

### 4. Hovers (подсказки)

При наведении на код показывает:
- Автор и дата
- Commit message
- Diff изменений
- Ссылки на GitHub (если настроено)

### 5. GitLens Sidebar

В боковой панели VS Code:
- **Commits** - последние коммиты
- **File History** - история текущего файла
- **Line History** - история выделенных строк
- **Branches** - все ветки
- **Remotes** - удаленные репозитории
- **Stashes** - сохраненные изменения
- **Tags** - теги
- **Contributors** - участники проекта

---

## 🔗 Интеграция с GitHub

GitLens автоматически интегрируется с GitHub, если ваш репозиторий на GitHub.

### Автоматические фичи:

1. **Pull Requests в коде:**
   - Видно связанные PR прямо в blame
   - Ссылки на PR discussions

2. **Avatars:**
   - Аватарки авторов в истории
   - Подгружаются с GitHub

3. **Links:**
   - Автоматические ссылки на коммиты в GitHub
   - Клик на коммит → открыть в браузере

### Настройка GitHub интеграции:

```json
{
  "gitlens.remotes": [
    {
      "domain": "github.com",
      "type": "GitHub"
    }
  ],
  "gitlens.integrations.enabled": true
}
```

Для доступа к приватным репозиториям:
1. `Ctrl+Shift+P` → "GitLens: Connect to GitHub"
2. Авторизуйтесь через браузер
3. GitLens получит доступ к вашим репозиториям

---

## 📊 Работа с большими проектами

### Проблема: GitLens замедляет VS Code

**Решения:**

1. **Отключить для больших файлов:**
```json
{
  "gitlens.advanced.blame.delayAfterEdit": 3000,
  "gitlens.advanced.maxListItems": 100
}
```

2. **Отключить inline blame:**
```json
{
  "gitlens.currentLine.enabled": false
}
```

3. **Использовать только Hovers:**
```json
{
  "gitlens.codeLens.enabled": false,
  "gitlens.currentLine.enabled": false,
  "gitlens.hovers.enabled": true
}
```

---

## 🆚 Альтернативы GitLens

Если GitLens не подходит:

### 1. Git History (by Don Jayamanne)
- Более простой и легковесный
- Фокус на истории коммитов
- Меньше функций, быстрее работает

### 2. Git Graph (by mhutchie)
- Только визуальный граф коммитов
- Минималистичный интерфейс
- Отличная производительность

### 3. Встроенный Git в VS Code
- Базовые функции
- Самый быстрый
- Достаточно для простых проектов

---

## ✅ Когда использовать GitLens

### Идеально подходит для:

- **Командная разработка** - видно кто и что менял
- **Code review** - удобный просмотр изменений
- **Debugging** - найти когда появился баг
- **Legacy код** - понять контекст старого кода
- **Документация** - отследить изменения в docs
- **Learning** - понять как работает Git визуально

### Примеры из DevTools:

1. **Скрипты PowerShell:**
   ```powershell
   # Открыть D:\DevTools\Scripts\create-python-project.ps1
   # Toggle blame (Alt+B)
   # Увидеть кто и когда добавлял каждый template
   ```

2. **Документация:**
   ```powershell
   # Открыть D:\DevTools\Docs\PYTHON_GUIDE.md
   # File History → посмотреть как менялся гайд
   # Сравнить версии
   ```

3. **Конфигурация:**
   ```powershell
   # Открыть D:\DevTools\Config\.env
   # Line History → кто добавлял переменные
   ```

---

## 🎓 Обучение Git через GitLens

GitLens отлично помогает понять Git визуально:

### 1. Как работают ветки
- Откройте Commit Graph
- Создайте ветку
- Сделайте коммит
- Посмотрите как изменился граф

### 2. Что такое merge
- Слейте две ветки
- В графе видно как ветки соединяются
- Посмотрите merge commit

### 3. Кто что менял
- Blame показывает авторов каждой строки
- Понятно как код эволюционировал
- Видно collaboration

---

## 🚀 Интеграция с DevTools workflows

### Workflow 1: Создание Python проекта

```powershell
# 1. Создайте проект
.\Scripts\create-python-project.ps1 my-app -Template fastapi

# 2. Откройте в VS Code
cd Database\my-app
code .

# 3. GitLens автоматически покажет:
#    - Initial commit от create-python-project.ps1
#    - Blame для сгенерированных файлов
#    - Timeline в боковой панели

# 4. Начните разработку
#    - Каждый ваш коммит появится в GitLens
#    - Используйте Graph для визуализации
```

### Workflow 2: Обновление документации

```powershell
# 1. Откройте D:\DevTools\Docs\
# 2. Измените гайд (например, PYTHON_GUIDE.md)
# 3. GitLens покажет:
#    - Какие строки изменены (vs HEAD)
#    - Previous version при hover
# 4. Commit с хорошим сообщением
# 5. В File History увидите новый коммит
```

### Workflow 3: Code Review для Pull Request

```powershell
# 1. Checkout ветка с PR
git fetch origin
git checkout feature/new-feature

# 2. Откройте GitLens Graph
# 3. Compare with main
# 4. Просмотрите все изменения в diff view
# 5. Комментируйте код
# 6. Approve или Request Changes
```

---

## ⚡ Pro Tips

### Tip 1: Быстрая навигация по истории
```
Alt+Left/Right - переключение между версиями файла
Работает как "назад/вперед" в браузере
```

### Tip 2: Копирование информации о коммите
```
Hover на строку → клик на commit hash → копируется в буфер
Удобно для commit messages и документации
```

### Tip 3: Сравнение веток перед merge
```
В Graph: правый клик на ветку → Compare with...
Увидите ВСЕ изменения перед слиянием
```

### Tip 4: Поиск по автору
```
Ctrl+Shift+P → GitLens: Search Commits
Введите: @author:name
Найдете все коммиты конкретного человека
```

### Tip 5: Ignore в .gitignore через GitLens
```
Файл в Source Control → правый клик → "Add to .gitignore"
GitLens автоматически добавит правило
```

---

## 📝 Резюме

### Плюсы GitLens:

✅ Мощная визуализация истории Git
✅ Inline blame - видно автора прямо в коде
✅ Commit Graph - наглядный граф веток
✅ GitHub интеграция из коробки
✅ Помогает понять Git визуально
✅ Ускоряет Code Review
✅ Бесплатный (с Pro версией для команд)

### Минусы GitLens:

⚠️ Может замедлять VS Code на больших проектах
⚠️ Много информации - перегружает интерфейс
⚠️ Кривая обучения - нужно время освоиться
⚠️ Некоторые фичи требуют GitLens+ (платно)

### Итоговая рекомендация для DevTools:

**ДА, УСТАНОВИТЕ!**

Для вашего случая GitLens идеален:
- Репозиторий на GitHub (ircitdev/devtools) ✓
- Много документации - нужна история изменений ✓
- Скрипты PowerShell - важно видеть кто и что менял ✓
- Обучение Git - визуализация помогает ✓

Начните с минимальной настройки, постепенно включайте фичи по мере освоения.

---

## 📞 Полезные ссылки

- **Официальный сайт:** https://gitlens.amod.io/
- **Документация:** https://help.gitkraken.com/gitlens/
- **VS Code Marketplace:** https://marketplace.visualstudio.com/items?itemName=eamodio.gitlens
- **GitHub:** https://github.com/gitkraken/vscode-gitlens

---

**Сделано для DevTools Environment**
Руководство актуально для GitLens v14+ и VS Code 1.80+
