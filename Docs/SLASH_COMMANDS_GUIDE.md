# Slash Commands для Claude Code - Руководство

> Автоматически создаваемые slash команды для ваших проектов

## 📋 Содержание

- [Что такое Slash Commands](#что-такое-slash-commands)
- [Как это работает](#как-это-работает)
- [Доступные команды](#доступные-команды)
- [Использование](#использование)
- [Создание своих команд](#создание-своих-команд)
- [Примеры](#примеры)

---

## 🎯 Что такое Slash Commands

Slash Commands - это предопределенные команды для Claude Code, которые позволяют быстро выполнять типичные задачи разработки.

Вместо того чтобы каждый раз писать "Создай тесты для этого кода", вы просто пишете `/test`.

### Преимущества:

- ⚡ **Быстрота** - одна команда вместо длинного запроса
- 🎯 **Точность** - команды настроены под конкретный тип проекта
- 📦 **Готовность** - команды создаются автоматически при создании проекта
- ✏️ **Гибкость** - легко создавать свои команды

---

## 🔧 Как это работает

### Автоматическое создание

Когда вы создаете новый проект с помощью `create-python-project.ps1`, скрипт автоматически:

1. Создает папку `.claude/commands/`
2. Копирует общие команды (analyze, document, test, review, fix)
3. Копирует специфичные для шаблона команды
4. Создает `.claude/README.md` с описанием

### Структура проекта

```
my-project/
├── .claude/
│   ├── commands/
│   │   ├── analyze.md      # Анализ кода
│   │   ├── document.md     # Документация
│   │   ├── test.md         # Тесты
│   │   ├── review.md       # Code review
│   │   ├── fix.md          # Исправление багов
│   │   ├── api.md          # (FastAPI) Создание endpoint
│   │   ├── model.md        # (FastAPI) Создание модели
│   │   └── migration.md    # (FastAPI) Миграция БД
│   └── README.md           # Описание команд
├── src/
├── tests/
└── ...
```

---

## 📚 Доступные команды

### Общие команды (все проекты)

#### `/analyze`
Анализ кода и предложения по улучшению.

**Что проверяет:**
- Качество кода и читаемость
- Производительность
- Безопасность
- Best practices
- Потенциальные баги

**Пример:**
```
/analyze src/main.py
```

---

#### `/document`
Создание или обновление документации.

**Что создает:**
- Docstrings (Google/NumPy style)
- Type hints
- Примеры использования
- Описание параметров и возвращаемых значений

**Пример:**
```
/document
```
*(для выбранного кода)*

---

#### `/test`
Генерация тестов используя pytest.

**Что включает:**
- Unit tests для всех функций
- Edge cases
- Happy path и error cases
- Fixtures
- Mocks для dependencies
- Параметризованные тесты

**Пример:**
```
/test src/api/users.py
```

---

#### `/review`
Code review текущих изменений.

**Что проверяет:**
- Best practices
- Безопасность (SQL injection, XSS, etc.)
- Производительность
- Обработка ошибок
- Качество тестов
- PEP 8 compliance

**Пример:**
```
/review
```
*(для текущих изменений)*

---

#### `/fix`
Исправление ошибок и багов.

**Что находит:**
- Syntax errors
- Runtime errors
- Logic errors
- Type errors
- Null references
- Index out of range

**Пример:**
```
/fix src/utils.py
```

---

### FastAPI проекты

#### `/api`
Создание нового REST API endpoint.

**Что создает:**
- Router definition
- Request/Response Pydantic models
- Validation
- Error handling
- OpenAPI documentation
- Тесты

**Пример:**
```
/api users CRUD
```

---

#### `/model`
Создание модели данных.

**Что создает:**
- Pydantic model (BaseModel)
- SQLAlchemy model (Base)
- Field validation
- Create/Update/Response schemas
- Relationships

**Пример:**
```
/model User
```

---

#### `/migration`
Создание миграции БД (Alembic).

**Что создает:**
- Migration file
- upgrade() функция
- downgrade() функция
- Правильные типы и constraints

**Пример:**
```
/migration add_users_table
```

---

### Flask проекты

Те же команды что и для FastAPI:
- `/api` - создание endpoint
- `/model` - создание модели
- `/migration` - миграция БД

---

### Data Science проекты

#### `/notebook`
Создание Jupyter notebook для анализа данных.

**Структура:**
1. Setup - импорты
2. Load Data - загрузка
3. Explore Data - первичный анализ
4. Clean Data - обработка
5. Transform Data - feature engineering
6. Visualize - графики
7. Analyze - статистика
8. Conclusions - выводы

**Пример:**
```
/notebook sales_analysis
```

---

### CLI проекты

#### `/cli-command`
Создание новой CLI команды (Typer).

**Что создает:**
- Typer app и команда
- Arguments и Options
- Help text
- Validation
- Rich вывод
- Error handling

**Пример:**
```
/cli-command export --format json
```

---

## 💡 Использование

### В Claude Code

1. **Откройте Claude Code** (Ctrl+L)
2. **Введите /**
3. **Выберите команду** из списка
4. **Добавьте контекст** если нужно

### Примеры использования

#### Пример 1: Создание тестов

```
# Выделите код в редакторе
# Затем в Claude Code:
/test

# Claude создаст тесты для выделенного кода
```

#### Пример 2: Code Review

```
# Сделали изменения в файле
# В Claude Code:
/review

# Claude проанализирует изменения
```

#### Пример 3: Создание API endpoint

```
# В Claude Code:
/api Create user registration endpoint

# Claude создаст:
# - Router
# - Pydantic models
# - Endpoint функцию
# - Тесты
```

#### Пример 4: Документация

```
# Выделите функцию без docstring
# В Claude Code:
/document

# Claude добавит docstring с описанием
```

---

## ✏️ Создание своих команд

### Структура команды

Каждая команда - это `.md` файл в `.claude/commands/`:

```markdown
---
description: Краткое описание команды
---

Подробная инструкция что должен делать Claude.

Включи:
- Пункт 1
- Пункт 2

Используй примеры и будь конкретен.
```

### Пример: Создание команды `/deploy`

1. **Создайте файл** `.claude/commands/deploy.md`:

```markdown
---
description: Подготовка к деплою
---

Проверь готовность проекта к деплою.

Выполни проверки:
- Все тесты проходят (pytest)
- Нет TODO/FIXME в коде
- requirements.txt обновлен
- .env.example актуален
- README.md содержит deployment инструкции
- Версия в package обновлена

Предложи checklist для деплоя.
```

2. **Используйте команду:**

```
/deploy
```

---

## 🎨 Примеры команд

### Пример 1: Команда для оптимизации

`.claude/commands/optimize.md`:

```markdown
---
description: Оптимизация производительности кода
---

Проанализируй код на производительность и предложи оптимизации.

Обрати внимание на:
- O(n) сложность алгоритмов
- Дублирование вычислений
- Ненужные циклы
- Использование pandas/numpy оптимально
- Database queries (N+1 problem)
- Кэширование

Предложи конкретные улучшения с замерами производительности.
```

### Пример 2: Команда для безопасности

`.claude/commands/security.md`:

```markdown
---
description: Проверка безопасности кода
---

Проведи security audit кода.

Проверь на:
- SQL injection
- XSS vulnerabilities
- CSRF protection
- Секреты в коде
- Слабые пароли/токены
- Небезопасная десериализация
- Path traversal
- CORS настройки

Используй OWASP Top 10 как reference.
```

### Пример 3: Команда для рефакторинга

`.claude/commands/refactor.md`:

```markdown
---
description: Рефакторинг кода
---

Прорефактори выбранный код следуя принципам SOLID.

Улучши:
- Названия переменных и функций
- Разбей большие функции
- Убери дублирование (DRY)
- Упрости сложную логику
- Добавь type hints
- Улучши читаемость

Сохрани функциональность и добавь тесты.
```

---

## 🔥 Best Practices

### 1. Используйте контекст

Slash команды работают лучше когда вы предоставляете контекст:

```
# ✅ Хорошо
/test src/api/users.py

# ✅ Еще лучше - выделите код
# Выделите функцию → /test

# ❌ Плохо - слишком общо
/test
```

### 2. Комбинируйте команды

```
# Сначала создайте код
/api users CRUD

# Затем создайте тесты
/test

# Потом code review
/review
```

### 3. Кастомизируйте под проект

Отредактируйте команды в `.claude/commands/` чтобы учесть специфику проекта:

```markdown
# В .claude/commands/test.md добавьте:

Используй фикстуры из tests/conftest.py.
Следуй паттернам из tests/test_base.py.
```

### 4. Создавайте проектные команды

Для каждого проекта могут быть уникальные команды:

- `/migrate` - запуск миграций
- `/seed` - наполнение БД тестовыми данными
- `/lint` - запуск всех linters
- `/build` - сборка проекта
- `/docs` - генерация документации

---

## 📖 Интеграция с DevTools

### Автоматическое создание при генерации проекта

```powershell
# При создании проекта команды создаются автоматически
.\Scripts\create-python-project.ps1 my-api -Template fastapi

# Проект создан с командами:
# - /analyze, /document, /test, /review, /fix
# - /api, /model, /migration (для FastAPI)
```

### Проверка созданных команд

```powershell
cd Database\my-api
ls .claude\commands

# Вывод:
# analyze.md
# document.md
# test.md
# review.md
# fix.md
# api.md
# model.md
# migration.md
```

### README в проекте

Каждый проект содержит `.claude/README.md` с описанием доступных команд:

```powershell
code .claude\README.md
```

---

## 🚀 Примеры реального использования

### Scenario 1: Создание FastAPI CRUD

```powershell
# 1. Создайте проект
.\Scripts\create-python-project.ps1 users-api -Template fastapi
cd Database\users-api
code .

# 2. В Claude Code:
/api Create users CRUD with endpoints: GET /users, POST /users, PUT /users/{id}, DELETE /users/{id}

# 3. Claude создаст endpoint'ы

# 4. Создайте тесты:
/test src/api/users.py

# 5. Code review:
/review

# 6. Запустите API и тесты
```

### Scenario 2: Data Analysis проект

```powershell
# 1. Создайте проект
.\Scripts\create-python-project.ps1 sales-analysis -Template data-science
cd Database\sales-analysis
code .

# 2. Создайте notebook:
/notebook Analyze monthly sales data from sales.csv

# 3. Claude создаст структурированный notebook

# 4. Добавьте документацию:
/document

# 5. После анализа - review:
/review
```

### Scenario 3: CLI Tool

```powershell
# 1. Создайте проект
.\Scripts\create-python-project.ps1 data-processor -Template cli
cd Database\data-processor
code .

# 2. Создайте команды:
/cli-command process --input file.csv --output result.json
/cli-command validate --schema schema.json

# 3. Создайте тесты:
/test

# 4. Документация:
/document
```

---

## 🔗 Связанные документы

- [CLAUDE_CODE_GUIDE.md](CLAUDE_CODE_GUIDE.md) - Полное руководство по Claude Code
- [PYTHON_PROJECT_GUIDE.md](PYTHON_PROJECT_GUIDE.md) - Создание Python проектов
- [BUILD_WITH_AGENT_GUIDE.md](BUILD_WITH_AGENT_GUIDE.md) - Build with Agent

---

## 📝 FAQ

### Как добавить новую команду?

1. Создайте `.md` файл в `.claude/commands/`
2. Добавьте frontmatter с description
3. Напишите инструкцию для Claude
4. Сохраните и используйте команду

### Можно ли изменить существующие команды?

Да! Просто отредактируйте соответствующий `.md` файл в `.claude/commands/`.

### Работают ли команды без интернета?

Нет, Claude Code требует подключения к интернету для работы.

### Можно ли поделиться командами между проектами?

Да! Скопируйте файлы из `.claude/commands/` одного проекта в другой.

### Как удалить команду?

Просто удалите соответствующий `.md` файл из `.claude/commands/`.

---

**Сделано для DevTools Environment**

Slash Commands автоматически создаются при использовании `create-python-project.ps1` и оптимизированы для работы с Python, FastAPI, Flask, Data Science и CLI проектами.
