# DevTools - Инструкция по установке

Полная среда разработки для Windows с Python, Node.js, Docker, и многим другим.

## 📋 Содержание

- [Системные требования](#системные-требования)
- [Быстрая установка](#быстрая-установка)
- [Подробная установка](#подробная-установка)
- [Проверка установки](#проверка-установки)
- [Что устанавливается](#что-устанавливается)
- [Настройка после установки](#настройка-после-установки)
- [Решение проблем](#решение-проблем)

---

## 📌 Системные требования

### Минимальные требования:
- **ОС:** Windows 10 (64-bit) или Windows 11
- **RAM:** 8 GB
- **Диск:** 10 GB свободного места
- **Процессор:** Dual-core 2.0 GHz+

### Рекомендуемые требования:
- **ОС:** Windows 11
- **RAM:** 16 GB
- **Диск:** 20 GB свободного места (SSD)
- **Процессор:** Quad-core 2.5 GHz+

### Обязательное ПО (устанавливается вручную):
- **Python 3.11+** - [Скачать](https://www.python.org/downloads/)
- **Node.js 18+** - [Скачать](https://nodejs.org/)
- **Git 2.0+** - [Скачать](https://git-scm.com/download/win)

### Опциональное ПО:
- **Docker Desktop** - [Скачать](https://www.docker.com/products/docker-desktop)
- **VS Code** - [Скачать](https://code.visualstudio.com/)

---

## 🚀 Быстрая установка

### Способ 1: Автоматическая установка (рекомендуется)

1. **Установите обязательное ПО:**
   - Python 3.11+
   - Node.js 18+
   - Git

2. **Скачайте установщик:**
   ```powershell
   # Откройте PowerShell от Администратора
   git clone https://github.com/ircitdev/devtools.git D:\DevTools
   cd D:\DevTools
   ```

3. **Запустите установку:**
   ```powershell
   .\install-devtools.ps1
   ```

Готово! Переходите к разделу [Проверка установки](#проверка-установки).

---

## 📖 Подробная установка

### Шаг 1: Установка обязательного ПО

#### 1.1. Python 3.11+

1. Скачайте с [python.org](https://www.python.org/downloads/)
2. Запустите установщик
3. **ВАЖНО:** Отметьте "Add Python to PATH"
4. Выберите "Install Now"
5. Проверьте установку:
   ```powershell
   python --version
   ```

#### 1.2. Node.js 18+

1. Скачайте LTS версию с [nodejs.org](https://nodejs.org/)
2. Запустите установщик
3. Примите настройки по умолчанию
4. Проверьте установку:
   ```powershell
   node --version
   npm --version
   ```

#### 1.3. Git

1. Скачайте с [git-scm.com](https://git-scm.com/download/win)
2. Запустите установщик
3. Рекомендуемые настройки:
   - Editor: VS Code (если установлен) или Nano
   - Path: "Git from the command line and also from 3rd-party software"
   - Line endings: "Checkout Windows-style, commit Unix-style"
4. Проверьте установку:
   ```powershell
   git --version
   ```

#### 1.4. Docker Desktop (опционально)

1. Скачайте с [docker.com](https://www.docker.com/products/docker-desktop)
2. Запустите установщик
3. Перезагрузите компьютер
4. Запустите Docker Desktop
5. Проверьте установку:
   ```powershell
   docker --version
   ```

---

### Шаг 2: Клонирование репозитория

```powershell
# Откройте PowerShell от Администратора
git clone https://github.com/ircitdev/devtools.git D:\DevTools
cd D:\DevTools
```

---

### Шаг 3: Запуск установщика

```powershell
# Разрешить выполнение скриптов
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Запустить установщик
.\install-devtools.ps1
```

Установщик автоматически:
- Проверит системные требования
- Создаст структуру директорий
- Установит Chocolatey и дополнительные пакеты
- Настроит Python виртуальное окружение
- Настроит Node.js глобальные пакеты
- Создаст Docker Compose файлы
- Настроит Git конфигурацию

**Время установки:** ~15-30 минут (зависит от скорости интернета)

---

### Шаг 4: Настройка компонентов

После установки запустите:

```powershell
cd D:\DevTools

# Настроить все компоненты
.\DevTools-Manager.ps1 setup all
```

Это настроит:
- Python venv + пакеты
- Node.js global modules
- Docker compose files
- Git templates

---

## ✅ Проверка установки

### Автоматическая проверка:

```powershell
cd D:\DevTools
.\DevTools-Manager.ps1 status
```

Вы должны увидеть:
```
[Python]
  Status: Configured
  Version: Python 3.11.x

[Node.js]
  Status: Installed
  Node: v22.x.x
  npm: 10.x.x

[Git]
  Status: Installed
  Version: git version 2.x.x

[Docker]
  Status: Installed
  Version: Docker version 28.x.x
```

### Ручная проверка:

```powershell
# Python
D:\DevTools\Python\venv\Scripts\python.exe --version

# Node.js
node --version
npm --version

# Git
git --version

# Docker (если установлен)
docker --version

# Chocolatey packages
choco list --local-only
```

---

## 📦 Что устанавливается

### Основные инструменты:

1. **Python Environment**
   - Python 3.11+ виртуальное окружение
   - 20+ пакетов (fastapi, pandas, jupyter, anthropic, и др.)
   - pip, setuptools, wheel

2. **Node.js Environment**
   - npm настроен на локальные модули
   - 9 глобальных пакетов (typescript, eslint, prettier, pm2, и др.)
   - yarn, pnpm

3. **Docker Configuration**
   - Compose файлы для PostgreSQL, MySQL, MongoDB, Redis
   - Скрипты управления контейнерами
   - Volumes для данных

4. **Git Configuration**
   - Шаблоны .gitconfig и .gitignore
   - Скрипты для GitHub интеграции

### Дополнительные инструменты (через Chocolatey):

- **DBeaver** - универсальный клиент БД
- **Postman** - API тестирование
- **Terraform** - Infrastructure as Code
- **jq** - JSON процессор
- **tree** - визуализация структуры
- **7-Zip** - архиватор

### Скрипты и утилиты:

- `DevTools-Manager.ps1` - главный менеджер
- `create-python-project.ps1` - создание Python проектов
- `create-node-project.ps1` - создание Node.js проектов
- `backup-devtools.ps1` - резервное копирование
- `update-all.ps1` - обновление пакетов
- И многие другие...

### Документация:

- README.md - основная документация
- QUICKSTART.md - быстрый старт
- PYTHON_PROJECT_QUICKSTART.md - Python проекты
- Docs/ - подробные руководства
  - PYTHON_GUIDE.md
  - NODEJS_GUIDE.md
  - DOCKER_GUIDE.md
  - JUPYTER_GUIDE.md
  - GITHUB_GUIDE.md
  - и другие...

---

## ⚙️ Настройка после установки

### 1. Настроить Git пользователя

```powershell
cd D:\DevTools
.\Git\setup-user.ps1
```

Или вручную:
```powershell
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
```

### 2. Настроить GitHub credentials (опционально)

Отредактируйте `D:\DevTools\Config\.env`:
```env
GITHUB_OWNER=your-username
GITHUB_TOKEN=your-github-token
```

### 3. Запустить Docker контейнеры (если Docker установлен)

```powershell
# Запустите Docker Desktop

# Затем:
.\DevTools-Manager.ps1 docker start
```

### 4. Добавить в PATH (опционально, но рекомендуется)

Добавьте в системную переменную PATH:
```
D:\DevTools\Scripts
D:\DevTools\Python\venv\Scripts
D:\DevTools\NodeJS\global_modules
```

**Как добавить:**
1. Win + R → `sysdm.cpl`
2. Advanced → Environment Variables
3. System Variables → Path → Edit
4. Add New → вставьте пути

### 5. Настроить VS Code (если установлен)

```powershell
cd D:\DevTools
code .
```

Установите расширения:
- Python (Microsoft)
- Claude Code (Anthropic)
- Pylance
- ESLint
- Prettier

---

## 🆘 Решение проблем

### Проблема: "Execution Policy" ошибка

**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: Python не найден

**Решение:**
1. Переустановите Python с галочкой "Add Python to PATH"
2. Или добавьте в PATH вручную: `C:\Python311`

### Проблема: npm не работает

**Решение:**
```powershell
# Переустановите глобальные пакеты
cd D:\DevTools
.\DevTools-Manager.ps1 setup nodejs
```

### Проблема: Docker daemon not running

**Решение:**
1. Запустите Docker Desktop
2. Дождитесь полного запуска (значок в трее станет зелёным)
3. Попробуйте снова

### Проблема: Chocolatey пакеты не устанавливаются

**Решение:**
```powershell
# Установите вручную
choco install dbeaver postman terraform jq tree 7zip -y
```

### Проблема: Недостаточно прав

**Решение:**
- Запустите PowerShell от Администратора
- Правый клик на PowerShell → "Run as Administrator"

### Проблема: Мало места на диске

**Решение:**
- Очистите диск (минимум 10GB свободного)
- Или измените путь установки:
  ```powershell
  .\install-devtools.ps1 -InstallPath "C:\DevTools"
  ```

---

## 🔄 Обновление

### Обновить DevTools:

```powershell
cd D:\DevTools
git pull origin main
.\DevTools-Manager.ps1 setup all
```

### Обновить все пакеты:

```powershell
.\Scripts\update-all.ps1
```

---

## 🗑️ Удаление

Если нужно полностью удалить DevTools:

```powershell
# 1. Остановить Docker контейнеры
.\DevTools-Manager.ps1 docker stop

# 2. Удалить папку
Remove-Item -Recurse -Force D:\DevTools

# 3. Удалить Chocolatey пакеты (опционально)
choco uninstall dbeaver postman terraform jq tree 7zip -y
```

---

## 📞 Поддержка

**Проблемы или вопросы?**

- GitHub Issues: https://github.com/ircitdev/devtools/issues
- Документация: `D:\DevTools\Docs\`

---

## 📜 Лицензия

MIT License

---

## 🙏 Благодарности

- Python Software Foundation
- Node.js Foundation
- Docker Inc.
- Chocolatey Software
- All package maintainers

---

**Готово! Начинайте разработку!** 🚀

**Следующие шаги:**
1. Прочитайте [QUICKSTART.md](QUICKSTART.md)
2. Создайте свой первый проект
3. Изучите документацию в `Docs/`
